# shellcheck shell=sh
rc_include rc/CC.rc rc/pkg-config.rc

prog="
#include <zmq.h>

void f() { int major, minor, patch; zmq_version(&major, &minor, &patch); }
"

rc_pkg_detect LIBZMQ "libzmq" \
  rc_compile cc link "$prog"
rc_save
